public class User {
  //3 variables to track
  String firstName, lastName;
  int id;

  //Contructs user attributes based on parameters passed
  public User(String fName, String lName, int identifierNumber){
    firstName = fName;
    lastName = lName;
    id = identifierNumber;
  }
}
